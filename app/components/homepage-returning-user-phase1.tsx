"use client"

// Phase 1 home screen — placeholder
// To build: V1 metrics row + V4 recent flows + chat simulator + navy theme
// Reference: homepage-returning-user.tsx (V1 metrics), homepage-returning-user-v4.tsx (flows + simulator)

export function HomepageReturningUserPhase1() {
  return (
    <div className="flex min-h-[calc(100vh-64px)] items-center justify-center">
      <div className="text-center">
        <p className="text-sm font-medium uppercase tracking-widest text-[#A64E8D]">Phase 1</p>
        <h1 className="mt-2 text-2xl font-semibold text-white">Home screen coming soon</h1>
        <p className="mt-2 text-sm text-white/50">V1 metrics + V4 recent flows + simulator + navy theme</p>
      </div>
    </div>
  )
}
